import { loadInventory } from "@/lib/inventory";
export default async function InventoryPage(){ const items=await loadInventory(); return <main className="shell"><nav className="nav"><a className="brand" href="/">Cigar Vault</a><div className="badge">{items.length} lots</div></nav><section className="section"><h1 style={{fontSize:52}}>Inventory</h1><p className="lede">One row per owned lot, with permanent IDs designed for Smartsheet synchronization.</p><div className="tableWrap"><table className="table"><thead><tr><th>ID</th><th>Cigar</th><th>Year</th><th>Qty</th><th>Status</th><th>Score</th></tr></thead><tbody>{items.map(i=><tr key={i.inventoryId}><td className="small">{i.inventoryId}</td><td><strong>{i.brand}</strong><div className="small">{i.line} · {i.vitola}</div></td><td>{i.vintage||"—"}</td><td>{i.currentQty??"—"}</td><td>{i.status||"—"}</td><td>{i.score??"—"}</td></tr>)}</tbody></table></div></section></main> }
