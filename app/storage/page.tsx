import { loadInventory } from "@/lib/inventory";
export const dynamic="force-dynamic";
export default async function StoragePage(){const items=await loadInventory();const grouped=new Map<string,typeof items>();for(const item of items){const key=item.storageLocationId||"Unassigned";grouped.set(key,[...(grouped.get(key)||[]),item])}const locations=[...grouped.entries()].sort((a,b)=>a[0]==="Unassigned"?1:b[0]==="Unassigned"?-1:a[0].localeCompare(b[0]));return <main className="shell"><nav className="nav"><a className="brand" href="/">Cigar Vault</a><div className="navLinks"><a href="/inventory">Inventory</a><a href="/collection-health">Collection health</a><a href="/records">Journal & value</a></div></nav><section className="section inventoryHeader"><div className="eyebrow">Physical collection</div><h1>Storage</h1><p className="lede">See where every cigar lives and find the lots that still need a home.</p></section><section className="storageGrid">{locations.map(([location,lots])=>{const quantity=lots.reduce((sum,item)=>sum+(item.currentQty||0),0);return <a className={`storageCard ${location==="Unassigned"?"attention":""}`} href={`/inventory?storage=${encodeURIComponent(location==="Unassigned"?"unassigned":location)}`} key={location}><div className="storageIcon">{location==="Unassigned"?"?":location.slice(0,2).toUpperCase()}</div><div><div className="eyebrow">{location==="Unassigned"?"Needs attention":"Storage location"}</div><h2>{location}</h2><p>{lots.length} lots · {quantity} known cigars</p></div><b>Open →</b></a>})}</section></main>}
