import { WorkspaceLoading } from "@/components/workspace-loading";

export default function LoadingRecords() {
  return <WorkspaceLoading label="Preparing your private journal" message="Preparing journal entries and value evidence…" layout="split"/>;
}
