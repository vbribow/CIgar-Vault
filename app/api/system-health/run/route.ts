import { NextResponse } from "next/server";
import { z } from "zod";
import { saveOwnedRecord } from "@/lib/user-data";
import { systemJobs,type SystemRun } from "@/lib/system-health";
const Input=z.object({jobId:z.enum(["sensor-sync","catalog-discovery","wishlist-monitor","valuation-monitor","rating-monitor"]),writeKey:z.string().min(1)});
export async function POST(request:Request){const startedAt=new Date().toISOString();let jobId:SystemRun["jobId"]="sensor-sync";try{const input=Input.parse(await request.json());jobId=input.jobId;const job=systemJobs.find(value=>value.id===jobId)!;const response=await fetch(new URL(job.path,request.url),{method:jobId==="sensor-sync"?"POST":"GET",headers:{"x-founder-key":input.writeKey,"content-type":"application/json"},cache:"no-store",signal:AbortSignal.timeout(290_000)});const result=await response.json();const completedAt=new Date().toISOString();const run:SystemRun={runId:`RUN-${jobId}-${completedAt}-${crypto.randomUUID()}`,jobId,status:response.ok?"Succeeded":"Failed",startedAt,completedAt,summary:response.ok?JSON.stringify(result.data||{}).slice(0,800):"Automation returned an error",error:response.ok?undefined:String(result.error||`HTTP ${response.status}`).slice(0,500)};if(!await saveOwnedRecord("system-runs",run.runId,run))return NextResponse.json({error:"Sign in to run and record system operations"},{status:401});return NextResponse.json({data:run},{status:response.ok?200:502})}catch(error){const completedAt=new Date().toISOString();const run:SystemRun={runId:`RUN-${jobId}-${completedAt}-${crypto.randomUUID()}`,jobId,status:"Failed",startedAt,completedAt,summary:"Automation could not run",error:error instanceof Error?error.message:"Run failed"};await saveOwnedRecord("system-runs",run.runId,run).catch(()=>false);return NextResponse.json({error:run.error,data:run},{status:422})}}
