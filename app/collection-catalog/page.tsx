import { collectionTemplates } from "@/lib/collection-templates";
import "./catalog.css";
export default function CollectionCatalogPage(){return <main className="shell"><nav className="nav"><a className="brand" href="/">Cigar Vault</a><div className="navLinks"><a href="/collections">My collections</a><span className="badge">Research catalog</span></div></nav><section className="inventoryHeader"><div><div className="eyebrow">Known collectible sets</div><h1>Collection catalog</h1><p className="lede">Every template now carries its own packaging, contents, rarity, imagery, and matching rule.</p></div><a className="button" href="/collections">Use a template</a></section><section className="templateGrid">{collectionTemplates.map(template=><article className="templateCard" key={template.templateId} style={{"--template-accent":template.accent} as React.CSSProperties}><div className="templateVisual">{template.imageUrl?<img src={template.imageUrl} alt={`${template.name} packaging`}/>:<div className="templateMonogram"><span>{template.maker}</span><strong>{template.name}</strong></div>}<span className={`collectionStatus ${template.researchStatus==="Verified"?"complete":"incomplete"}`}>{template.researchStatus}</span></div><div className="templateBody"><div className="eyebrow">{template.maker} · {template.releaseYear||"Current"}</div><h2>{template.name}</h2><p className="templateEdition">{template.edition}</p><div className="templateFacts"><div><span>Packaging</span><strong>{template.packaging}</strong></div><div><span>Rarity</span><strong>{template.releaseQuantity||"Release quantity pending"}</strong></div><div><span>Component types</span><strong>{template.expectedComponents}</strong></div><div><span>Total cigars</span><strong>{template.expectedCigars??"Varies"}</strong></div></div><h3>Required contents</h3><ul className="templateChecklist">{template.requirements.map(item=><li key={item}><i>✓</i><span>{item}</span></li>)}</ul><div className="matchingRule"><span>Inventory matching rule</span><p>{template.matchingRule}</p></div><a className="textLink" href={template.sourceUrl} target="_blank" rel="noreferrer">Verify with {template.sourceLabel} ↗</a></div></article>)}</section></main>}
