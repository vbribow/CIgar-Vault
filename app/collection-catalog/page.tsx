import { collectionTemplates } from "@/lib/collection-templates";
import "./catalog.css";
export default function CollectionCatalogPage(){return <main className="shell"><nav className="nav"><a className="brand" href="/">Cigar Vault</a><div className="navLinks"><a href="/collections">My collections</a><span className="badge">Research catalog</span></div></nav><section className="inventoryHeader"><div><div className="eyebrow">Known collectible sets</div><h1>Collection catalog</h1><p className="lede">Source-backed templates that make adding a recognized release faster and more accurate.</p></div><a className="button" href="/collections">Build a collection</a></section><section className="templateGrid">{collectionTemplates.map(template=><article className="card" key={template.templateId}><div className="collectionTop"><span className={`collectionStatus ${template.researchStatus==="Verified"?"complete":"incomplete"}`}>{template.researchStatus}</span><small>{template.releaseYear||"Multi-year"}</small></div><div className="eyebrow">{template.maker}</div><h2>{template.name}</h2><p className="small">{template.edition}</p><ul>{template.requirements.map(item=><li key={item}>{item}</li>)}</ul><div className="templateMeta"><span>{template.expectedComponents} component types</span>{template.expectedCigars&&<span>{template.expectedCigars} cigars</span>}</div><a className="textLink" href={template.sourceUrl} target="_blank" rel="noreferrer">{template.sourceLabel} ↗</a></article>)}</section></main>}
