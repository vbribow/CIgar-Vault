"use client";
import{useState,type FormEvent}from"react";import type{InventoryItem}from"@/lib/types";
type Row={row:number;item?:InventoryItem;errors:string[];warnings:string[];duplicate:boolean};type Preview={fileName:string;rows:Row[];valid:number;invalid:number;duplicates:number;columns:string[]};
export function InventoryFileImport(){const[preview,setPreview]=useState<Preview>(),[selected,setSelected]=useState<Set<number>>(new Set()),[message,setMessage]=useState(""),[busy,setBusy]=useState(false),[batch,setBatch]=useState("");async function inspect(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage("");const response=await fetch("/api/account/import-inventory",{method:"POST",body:new FormData(event.currentTarget)}),result=await response.json();setBusy(false);if(!response.ok){setMessage(result.error||"Preview failed");return}setPreview(result.data);setSelected(new Set(result.data.rows.filter((row:Row)=>row.item&&!row.duplicate).map((row:Row)=>row.row)))}async function commit(){if(!preview)return;setBusy(true);const items=preview.rows.filter(row=>row.item&&selected.has(row.row)).map(row=>row.item),response=await fetch("/api/account/import-inventory",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"commit",fileName:preview.fileName,items})}),result=await response.json();setBusy(false);if(!response.ok){setMessage(result.error||"Import failed");return}setBatch(result.data.batchId);setMessage(`${result.data.imported} inventory lots imported safely.`)}async function rollback(){const response=await fetch("/api/account/import-inventory",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action:"rollback",batchId:batch})}),result=await response.json();if(!response.ok){setMessage(result.error||"Rollback failed");return}setMessage(`${result.data.removed} imported lots removed.`);setBatch("");setPreview(undefined)}return <section className="card inventoryFileImport"><div><div className="eyebrow">Secure inventory migration</div><h2>Import Excel or CSV</h2><p>Upload one `.xlsx` worksheet or CSV. Cigar Vault rejects macros, formulas, links, embedded objects, and unsupported files. Nothing is saved until you review and confirm.</p></div><form onSubmit={inspect}><input name="file" type="file" accept=".xlsx,.csv,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" required/><button className="button" disabled={busy}>{busy?"Inspecting…":"Preview inventory"}</button></form>{message&&<output>{message}</output>}{preview&&<div className="importPreview"><p><strong>{preview.valid} valid</strong> · {preview.invalid} need correction · {preview.duplicates} possible duplicates</p><div className="tableWrap"><table className="table"><thead><tr><th>Use</th><th>Row</th><th>Cigar</th><th>Quantity</th><th>Review</th></tr></thead><tbody>{preview.rows.slice(0,200).map(row=><tr key={row.row}><td><input type="checkbox" disabled={!row.item} checked={selected.has(row.row)} onChange={event=>setSelected(current=>{const next=new Set(current);event.target.checked?next.add(row.row):next.delete(row.row);return next})}/></td><td>{row.row}</td><td>{row.item?<><strong>{row.item.brand}</strong><small>{row.item.line} · {row.item.vitola}</small></>:"Invalid row"}</td><td>{row.item?.currentQty??"—"}</td><td>{[...row.errors,...row.warnings].join("; ")||"Ready"}</td></tr>)}</tbody></table></div>{preview.rows.length>200&&<small>Showing the first 200 rows; all selected valid rows will be imported.</small>}<button type="button" className="button" disabled={busy||!selected.size||Boolean(batch)} onClick={commit}>Import {selected.size} reviewed rows</button>{batch&&<button type="button" className="button secondary" onClick={rollback}>Undo this import</button>}</div>}</section>}
