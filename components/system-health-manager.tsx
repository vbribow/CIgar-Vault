"use client";
import { useState } from "react";
import { latestRuns,systemJobs,type SystemRun } from "@/lib/system-health";
export function SystemHealthManager({initialRuns}:{initialRuns:SystemRun[]}){const[runs,setRuns]=useState(initialRuns);const[key,setKey]=useState("");const[busy,setBusy]=useState("");const[message,setMessage]=useState("");const latest=latestRuns(runs);async function run(jobId:SystemRun["jobId"]){const job=systemJobs.find(value=>value.id===jobId)!;if(!confirm(`Run ${job.name} now? This may call external services and update vault records.`))return;setBusy(jobId);setMessage("");try{const response=await fetch("/api/system-health/run",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({jobId,writeKey:key})});const result=await response.json();if(result.data)setRuns(current=>[result.data,...current]);if(!response.ok)throw new Error(result.error||"Run failed");setMessage(`${job.name} completed successfully.`)}catch(error){setMessage(error instanceof Error?error.message:"Run failed")}finally{setBusy("")}}return <section className="healthOperations"><div className="operationsToolbar"><div><div className="eyebrow">Operational controls</div><h2>Scheduled automation</h2><p>Run a job safely and preserve its result in the private run ledger.</p></div><label><span>Founder write key</span><input type="password" value={key} onChange={event=>setKey(event.target.value)} placeholder="Required to run now"/></label></div>{message&&<output>{message}</output>}<div className="jobGrid">{systemJobs.map(job=>{const last=latest.get(job.id);return <article className={last?.status.toLowerCase()||"never"} key={job.id}><div><span>{job.nextDescription}</span><strong>{job.name}</strong><small>{job.schedule}</small></div><div className="jobLast"><b>{last?.status||"Never run manually"}</b><span>{last?new Date(last.completedAt).toLocaleString():"No recorded result"}</span>{last?.error&&<small>{last.error}</small>}</div><button className="button secondary" disabled={!key||Boolean(busy)} onClick={()=>run(job.id)}>{busy===job.id?"Running…":"Run now"}</button></article>})}</div><section className="runLedger"><div className="sectionHead"><div><div className="eyebrow">Recent operations</div><h2>Run ledger</h2></div></div>{runs.slice(0,12).map(run=><article key={run.runId}><span className={run.status.toLowerCase()}>{run.status}</span><strong>{systemJobs.find(job=>job.id===run.jobId)?.name}</strong><small>{new Date(run.completedAt).toLocaleString()}</small><p>{run.error||run.summary}</p></article>)}{!runs.length&&<div className="emptyState">No manually triggered automation runs have been recorded yet.</div>}</section></section>}
