"use client";
import { useState } from "react";
import type { AvailabilityListing,WishlistItem } from "@/lib/types";
const money=new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:2});
export function WishlistAvailabilityBoard({items}:{items:WishlistItem[]}){const[results,setResults]=useState<Record<string,{checkedAt:string;summary:string;legalCaution:string;listings:AvailabilityListing[]}>>(()=>Object.fromEntries(items.filter(item=>item.availabilityLastCheckedAt).map(item=>[item.wishlistId,{checkedAt:item.availabilityLastCheckedAt!,summary:"Saved availability results",legalCaution:"Availability changes. Confirm the listing and applicable laws before purchasing.",listings:item.availabilityListings||[]}])));const[loading,setLoading]=useState<string>();const[error,setError]=useState("");async function search(item:WishlistItem){setLoading(item.wishlistId);setError("");const response=await fetch("/api/wishlist-availability",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({wishlistId:item.wishlistId})});const result=await response.json();setLoading(undefined);if(!response.ok){setError(result.error||"Search failed");return}setResults(current=>({...current,[item.wishlistId]:result.data}))}const watching=items.filter(item=>item.status==="Watching");return <section className="availabilityBoard"><div className="sectionHead"><div><div className="eyebrow">Live market leads</div><h2>Availability search</h2><p>Direct listings are research leads. Always confirm price, stock, authenticity, shipping, and applicable law with the seller.</p></div></div>{error&&<output className="wishlistMessage">{error}</output>}<div className="availabilityTargets">{watching.map(item=>{const research=results[item.wishlistId];return <article key={item.wishlistId}><header><div><small>{item.brand} · {item.line}</small><h3>{item.vitola}</h3>{research&&<span>Checked {new Date(research.checkedAt).toLocaleString()}</span>}</div><button className="button secondary" disabled={Boolean(loading)} onClick={()=>search(item)}>{loading===item.wishlistId?"Searching…":research?"Refresh search":"Search availability"}</button></header>{research&&<><p className="availabilitySummary">{research.summary}</p><div className="listingGrid">{research.listings.map(listing=><a href={listing.url} target="_blank" rel="noreferrer" key={listing.url}><span className={`availabilityState ${listing.availability.toLowerCase().replaceAll(" ","-")}`}>{listing.availability}</span><small>{listing.sellerType} · {listing.seller}</small><strong>{listing.title}</strong><div><b>{listing.askingPrice===undefined?"Price unavailable":money.format(listing.askingPrice)}</b><em>{listing.quantity?`${listing.quantity} cigars`:"Quantity unconfirmed"}{listing.unitPrice!==undefined?` · ${money.format(listing.unitPrice)} each`:""}</em></div><p>{listing.condition||listing.notes}</p></a>)}{!research.listings.length&&<div className="emptyState">No reliable direct listings were found.</div>}</div><small className="legalCaution">{research.legalCaution}</small></>}</article>})}{!watching.length&&<div className="emptyState">Add or reopen a watching target to search current availability.</div>}</div></section>}
