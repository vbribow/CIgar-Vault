"use client";
import{useEffect,useRef,useState}from"react";import type{SearchResult}from"@/lib/universal-search";
export function GlobalSearch(){const[open,setOpen]=useState(false),[query,setQuery]=useState(""),[results,setResults]=useState<SearchResult[]>([]),[loading,setLoading]=useState(false);const input=useRef<HTMLInputElement>(null);useEffect(()=>{const key=(event:KeyboardEvent)=>{if((event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==="k"){event.preventDefault();setOpen(true)}if(event.key==="Escape")setOpen(false)};window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[]);useEffect(()=>{if(open)window.setTimeout(()=>input.current?.focus(),0)},[open]);useEffect(()=>{if(query.trim().length<2){setResults([]);return}const controller=new AbortController(),timer=window.setTimeout(async()=>{setLoading(true);try{const response=await fetch(`/api/search?q=${encodeURIComponent(query)}`,{signal:controller.signal}),value=await response.json();if(response.ok)setResults(value.data??[])}catch(error){if((error as Error).name!=="AbortError")setResults([])}finally{setLoading(false)}},180);return()=>{window.clearTimeout(timer);controller.abort()}},[query]);return <><button className="globalSearchTrigger" onClick={()=>setOpen(true)} aria-label="Search Cigar Vault"><span>⌕</span><b>Search</b><kbd>⌘ K</kbd></button>{open&&<div className="commandOverlay" role="dialog" aria-modal="true" aria-label="Search Cigar Vault" onMouseDown={event=>{if(event.currentTarget===event.target)setOpen(false)}}><section className="commandPalette"><header><span>⌕</span><input ref={input} value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search cigars, collections, markets, or tools…"/><button onClick={()=>setOpen(false)} aria-label="Close search">×</button></header><div className="commandResults">{results.map(result=><a href={result.href} key={result.id}><span className="resultKind">{result.kind}</span><div><strong>{result.label}</strong><small>{result.detail}</small></div>{result.signal&&<b className="signalBadge">{result.signal}</b>}<em>→</em></a>)}{loading&&<p>Searching the vault…</p>}{!loading&&query.length>=2&&!results.length&&<p>No matching records or workspaces.</p>}{query.length<2&&<p>Type at least two characters. Try a brand, vitola, collection, box code, or workspace.</p>}</div><footer><span>Private account search</span><span>Esc to close</span></footer></section></div>}</>}
