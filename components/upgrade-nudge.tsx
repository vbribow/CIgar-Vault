"use client";import{useEffect}from"react";import{upgradeSuggestion,type CollectorSignals,type PlanId,type UpgradeContext}from"@/lib/entitlements";
function record(eventType:string,properties:Record<string,string>){void fetch("/api/product-events",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({eventType,properties})})}
export function UpgradeNudge({plan,context,usage=0,signals}:{plan?:PlanId;context:UpgradeContext;usage?:number;signals?:CollectorSignals}){const suggestion=plan?upgradeSuggestion(plan,context,usage,signals):undefined;useEffect(()=>{if(suggestion&&plan)record("upgrade-impression",{context,currentPlan:plan,targetPlan:suggestion.target})},[context,plan,suggestion?.target]);if(!plan||!suggestion)return null;const properties={context,currentPlan:plan,targetPlan:suggestion.target};return <aside className="upgradeNudge"><div><span>Collection stewardship</span><strong>{suggestion.title}</strong><small>{suggestion.detail}</small></div><a className="button secondary" href={`/pricing?recommended=${suggestion.target}`} onClick={()=>record("upgrade-clicked",properties)}>{suggestion.action}</a></aside>}
