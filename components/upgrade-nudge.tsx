import{upgradeSuggestion,type CollectorSignals,type PlanId,type UpgradeContext}from"@/lib/entitlements";
export function UpgradeNudge({plan,context,usage=0,signals}:{plan?:PlanId;context:UpgradeContext;usage?:number;signals?:CollectorSignals}){if(!plan)return null;const suggestion=upgradeSuggestion(plan,context,usage,signals);if(!suggestion)return null;return <aside className="upgradeNudge"><div><span>Collection stewardship</span><strong>{suggestion.title}</strong><small>{suggestion.detail}</small></div><a className="button secondary" href={`/pricing?recommended=${suggestion.target}`}>{suggestion.action}</a></aside>}
