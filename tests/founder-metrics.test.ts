import assert from"node:assert/strict";import test from"node:test";import{buildFounderMetrics}from"../lib/founder-metrics";
test("measures founder activation without exposing collector details",()=>{const records=[...Array.from({length:20},(_,index)=>({user_id:"one",kind:"inventory",record_id:String(index)})),{user_id:"one",kind:"smokes",record_id:"s1"},{user_id:"two",kind:"inventory",record_id:"i1"}];const metrics=buildFounderMetrics(["one","two","three"],records,[{user_id:"one",event_type:"insurance-report-viewed",created_at:"2026-07-21"}]);assert.deepEqual({total:metrics.total,imported:metrics.imported,activated:metrics.activated,smoked:metrics.smoked,reportViewed:metrics.reportViewed},{total:3,imported:2,activated:1,smoked:1,reportViewed:1});assert.equal(JSON.stringify(metrics).includes("userId"),false)});
test("measures unique upgrade interest without collector identity",()=>{const metrics=buildFounderMetrics(["one","two"],[],[{user_id:"one",event_type:"upgrade-impression",created_at:"2026-07-21"},{user_id:"one",event_type:"upgrade-impression",created_at:"2026-07-21"},{user_id:"one",event_type:"upgrade-clicked",created_at:"2026-07-21"},{user_id:"one",event_type:"pricing-viewed",created_at:"2026-07-21"}]);assert.equal(metrics.upgradeImpressions,1);assert.equal(metrics.upgradeClicks,1);assert.equal(metrics.pricingViews,1);assert.equal(metrics.upgradeClickRate,100)});
