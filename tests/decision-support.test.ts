import assert from"node:assert/strict";import test from"node:test";import{blendAgingGuidance,buildCollectionGoals,buildMarketChannels,evaluatePurchase}from"../lib/decision-support";import type{CigarCollection,InventoryItem,WishlistItem}from"../lib/types";
const inventory:InventoryItem[]=[{inventoryId:"1",brand:"Arturo Fuente",line:"OpusX",vitola:"PerfecXion X",vintage:2018,currentQty:10,retailValue:50,collectionId:"COL-1"}];
const collections:CigarCollection[]=[{collectionId:"COL-1",name:"Opus Set",expectedComponents:2,status:"Incomplete"}];
test("purchase advisor penalizes duplicate inventory and price premiums",()=>{const result=evaluatePurchase({brand:"Arturo Fuente",line:"OpusX",vitola:"PerfecXion X",askingPrice:100,quantity:1},{inventory,valuations:[],collections,smokes:[]});assert.equal(result.existingQuantity,10);assert.ok(result.cautions.some(value=>value.includes("already own")));assert.ok(result.cautions.some(value=>value.includes("above")))});
test("collection goals show progress and next action",()=>{const [goal]=buildCollectionGoals(collections,inventory,[]);assert.equal(goal.progress,50);assert.equal(goal.status,"Building");assert.ok(goal.nextAction)});
test("blend aging uses a transparent named profile",()=>{const result=blendAgingGuidance(inventory[0],new Date("2026-01-01T00:00:00Z"));assert.equal(result.phase,"In profile window");assert.match(result.basis,/Fuente/)});
test("market channels preserve auction, regional, and normalized price evidence",()=>{const wishlist:WishlistItem[]=[{wishlistId:"W1",brand:"Cohiba",line:"Siglo",vitola:"IV",priority:"High",status:"Watching",createdAt:"2026-01-01",availabilityListings:[{seller:"Auction House",sellerType:"Auction",title:"Lot",url:"https://example.co.uk/lot",availability:"Auction open",unitPrice:80,notes:""}]}];const market=buildMarketChannels(wishlist);assert.equal(market.auctionLots,1);assert.equal(market.groups[0].medianUnit,80);assert.match(market.groups[0].channel,/UK/)});
