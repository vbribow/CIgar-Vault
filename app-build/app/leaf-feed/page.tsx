import { loadCommunityPostSummaries } from "@/lib/community-pulse-store";
import { loadCollections,loadSmokingLogs,loadValuations } from "@/lib/data";
import { loadInventory } from "@/lib/inventory";
import { buildLeafFeed } from "@/lib/leaf-feed";
import "./leaf-feed.css";
export const dynamic="force-dynamic";
const groups=["Journal","Market","Collection","Cellar","Culture"] as const;
export default async function LeafFeedPage(){const results=await Promise.allSettled([loadInventory(),loadSmokingLogs(),loadValuations(),loadCollections(),loadCommunityPostSummaries()] as const);const ready=results.slice(0,4).every(result=>result.status==="fulfilled");if(!ready)return <main className="shell leafFeedPage"><section className="leafFeedHero"><div className="eyebrow">Leaf Feed protected</div><h1>Your connected view is temporarily paused.</h1><p className="lede">Hojavía could not verify every private source needed to personalize this feed. Partial data is not being presented as a complete collection view.</p><a className="button secondary" href="/leaf-feed">Check again</a></section></main>;const[inventory,smokes,valuations,collections,posts]=results.map(result=>result.status==="fulfilled"?result.value:[]) as[Awaited<ReturnType<typeof loadInventory>>,Awaited<ReturnType<typeof loadSmokingLogs>>,Awaited<ReturnType<typeof loadValuations>>,Awaited<ReturnType<typeof loadCollections>>,Awaited<ReturnType<typeof loadCommunityPostSummaries>>];const items=buildLeafFeed({inventory,smokes,valuations,collections,communityPosts:posts});return <main className="shell wideShell leafFeedPage"><section className="leafFeedHero"><div><div className="eyebrow">Your Leaf Feed</div><h1>Everything meaningful, brought into context.</h1><p className="lede">A private, evidence-aware view of journal history, collection progress, market movement, learning, and collector culture—without turning activity into noise.</p></div><aside><strong>{items.length}</strong><span>relevant signals</span><small>Private collection context · community facts labeled separately</small></aside></section><nav className="feedFilters" aria-label="Leaf Feed sections">{groups.map(group=><a key={group} href={`#feed-${group.toLowerCase()}`}>{group}</a>)}</nav>{groups.map(group=>{const values=items.filter(item=>item.kind===group);return values.length?<section className="feedSection" id={`feed-${group.toLowerCase()}`} key={group}><div className="sectionHead"><div><div className="eyebrow">{group}</div><h2>{group==="Market"?"Evidence that moved—not prices without context.":group==="Culture"?"Knowledge carried through people.":`${group} signals`}</h2></div></div><div className="feedGrid">{values.map(item=><a href={item.href} key={item.id}><span>{item.kind}</span><h3>{item.title}</h3><p>{item.detail}</p><b>{item.label} →</b></a>)}</div></section>:null})}{!items.length&&<div className="emptyState"><strong>Your Leaf Feed will take shape with your collection.</strong><p>Document a cigar, log an experience, or begin a collection goal. Nothing is inferred from an empty or unavailable source.</p></div>}<p className="feedDisclosure">Private collection signals, community experience, published evidence, and general education remain visibly distinct. Hojavía does not convert popularity, asking prices, or incomplete data into fact.</p></main>}
