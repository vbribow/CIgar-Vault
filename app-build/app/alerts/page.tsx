import { AlertTestPanel } from "@/components/alert-test-panel";
import { notificationConfiguration } from "@/lib/alert-notifications";
import { getAlertDeliveries } from "@/lib/smartsheet";
import type { AlertDelivery } from "@/lib/types";
import "./alerts.css";
export const dynamic="force-dynamic";
export default async function AlertsPage(){const config=notificationConfiguration();const history:AlertDelivery[]=config.history?await getAlertDeliveries():[];return <main className="shell"><section className="alertHero"><div><div className="eyebrow">Climate notifications</div><h1>Know before damage happens.</h1><p className="lede">Deliver critical climate and sensor warnings by email or text, then preserve proof of every alert.</p></div><div className="alertReadiness"><strong>{config.email||config.sms?"Delivery ready":"Setup required"}</strong><span>{[config.email&&"Email",config.sms&&"Text",config.history&&"History"].filter(Boolean).join(" · ")||"Add notification credentials and alert history storage"}</span></div></section><section className="channelGrid"><article className={config.email?"ready":""}><span>Email</span><strong>{config.email?"Ready":"Not configured"}</strong><small>Transactional delivery through Resend</small></article><article className={config.sms?"ready":""}><span>Text message</span><strong>{config.sms?"Ready":"Not configured"}</strong><small>Urgent SMS delivery through Twilio</small></article><article className={config.history?"ready":""}><span>Alert history</span><strong>{config.history?"Ready":"Not configured"}</strong><small>Deduplication and audit trail in Smartsheet</small></article></section><AlertTestPanel email={config.email} sms={config.sms}/><section className="alertHistory"><div className="sectionHead"><div><div className="eyebrow">Delivery ledger</div><h2>{history.length} recorded alerts</h2></div><a className="textLink" href="/humidors">Climate command center →</a></div>{history.length?<div className="tableWrap"><table className="table"><thead><tr><th>Detected</th><th>Humidor</th><th>Severity</th><th>Alert</th><th>Email</th><th>Text</th><th>Status</th></tr></thead><tbody>{history.slice(0,100).map(alert=><tr key={alert.alertId}><td>{alert.detectedAt.replace("T"," ")}</td><td>{alert.humidorId}</td><td><span className={`alertSeverity ${alert.severity.toLowerCase()}`}>{alert.severity}</span></td><td><strong>{alert.alertType}</strong><small>{alert.message}</small></td><td>{alert.emailSentAt?"Sent":"—"}</td><td>{alert.smsSentAt?"Sent":"—"}</td><td>{alert.status}</td></tr>)}</tbody></table></div>:<div className="emptyState">No alert deliveries yet. Alerts will appear here after notification storage is configured.</div>}</section></main>}
