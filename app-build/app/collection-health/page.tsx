import { loadInventory } from "@/lib/inventory";
import { inventoryCompleteness } from "@/lib/inventory-model";
export const dynamic="force-dynamic";
export default async function CollectionHealth(){const items=await loadInventory();const checks=[{key:"quantity",label:"Original quantity",missing:items.filter(i=>i.originalQty===undefined)},{key:"value",label:"Replacement value",missing:items.filter(i=>i.retailValue===undefined)},{key:"vintage",label:"Production year",missing:items.filter(i=>i.vintage===undefined)},{key:"storage",label:"Storage location",missing:items.filter(i=>!i.storageLocationId)},{key:"provenance",label:"Provenance notes",missing:items.filter(i=>!i.provenanceNotes)}];const average=Math.round(items.reduce((sum,item)=>sum+inventoryCompleteness(item),0)/Math.max(items.length,1));const ready=items.filter(i=>inventoryCompleteness(i)===100).length;return <main className="shell"><nav className="nav"><a className="brand" href="/">Cedriva</a><div className="navLinks"><a href="/inventory">Inventory</a><a href="/storage">Storage</a><a href="/records">Journal & value</a></div></nav><section className="healthHero"><div><div className="eyebrow">Collection intelligence</div><h1>Know what you know.</h1><p className="lede">Complete records produce trustworthy quantities, values, aging guidance, and recommendations.</p></div><div className="healthScore"><strong>{average}%</strong><span>collection completeness</span><small>{ready} of {items.length} lots launch-ready</small></div></section><section className="healthGrid">{checks.map(check=><a className="healthCard" href={`/inventory?missing=${check.key}`} key={check.key}><div><span>{check.label}</span><strong>{check.missing.length}</strong></div><p>{check.missing.length?`${check.missing.length} lots need attention`:`Complete across the collection`}</p><b>{check.missing.length?"Review records →":"Complete ✓"}</b></a>)}</section><section className="section"><div className="sectionHead"><div><div className="eyebrow">Highest impact</div><h2>Complete these next</h2></div><a className="button secondary" href="/inventory?missing=quantity">Start cleanup</a></div><div className="cleanupList">{[...items].sort((a,b)=>inventoryCompleteness(a)-inventoryCompleteness(b)).slice(0,8).map(item=><a href={`/inventory/${item.inventoryId}`} key={item.inventoryId}><span><strong>{item.brand} {item.line}</strong><small>{item.inventoryId} · {item.vitola}</small></span><span className="completionMeter"><i style={{width:`${inventoryCompleteness(item)}%`}}/><b>{inventoryCompleteness(item)}%</b></span></a>)}</div></section></main>}
