import{NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";import{authorizeWrite}from"@/lib/config";import{BetaCollectorInput}from"@/lib/beta-onboarding";
function admin(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL?.trim(),key=process.env.SUPABASE_SERVICE_ROLE_KEY?.trim();if(!url||!key)throw new Error("Founder onboarding requires Supabase service credentials");return createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}
const shape=(row:Record<string,unknown>)=>({id:String(row.id),name:String(row.name),email:String(row.email),stage:String(row.stage),notes:row.notes?String(row.notes):undefined,invitedAt:row.invited_at?String(row.invited_at):undefined,lastContactAt:row.last_contact_at?String(row.last_contact_at):undefined,createdAt:String(row.created_at),updatedAt:String(row.updated_at)});
export async function GET(request:Request){if(!authorizeWrite(request))return NextResponse.json({error:"Founder authorization required"},{status:401});try{const{data,error}=await admin().from("beta_collectors").select("*").order("updated_at",{ascending:false});if(error)throw error;return NextResponse.json({data:(data||[]).map(shape)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Unable to load onboarding queue"},{status:502})}}
export async function POST(request:Request){if(!authorizeWrite(request))return NextResponse.json({error:"Founder authorization required"},{status:401});try{const input=BetaCollectorInput.parse(await request.json());const now=new Date().toISOString();const{data,error}=await admin().from("beta_collectors").insert({name:input.name,email:input.email.toLowerCase(),stage:input.stage,notes:input.notes||null,invited_at:input.stage==="Invited"?input.invitedAt||now:input.invitedAt||null,last_contact_at:input.lastContactAt||null,updated_at:now}).select().single();if(error)throw error;return NextResponse.json({data:shape(data)},{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Invalid collector"},{status:422})}}
export async function PATCH(request:Request){if(!authorizeWrite(request))return NextResponse.json({error:"Founder authorization required"},{status:401});try{const input=BetaCollectorInput.extend({id:BetaCollectorInput.shape.id.unwrap()}).parse(await request.json());const now=new Date().toISOString();const{data,error}=await admin().from("beta_collectors").update({name:input.name,email:input.email.toLowerCase(),stage:input.stage,notes:input.notes||null,invited_at:input.stage==="Invited"&&!input.invitedAt?now:input.invitedAt||null,last_contact_at:input.lastContactAt||null,updated_at:now}).eq("id",input.id).select().single();if(error)throw error;return NextResponse.json({data:shape(data)})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Invalid collector update"},{status:422})}}
