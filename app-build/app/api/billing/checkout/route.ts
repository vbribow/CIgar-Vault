import { createClient, supabaseConfigured } from "@/lib/supabase/server";
import { billingConfigured, stripeHeaders } from "@/lib/billing";
import { NextResponse } from "next/server";
export async function POST(request:Request){const origin=new URL(request.url).origin;if(!supabaseConfigured())return NextResponse.redirect(`${origin}/login?error=${encodeURIComponent("Authentication is not configured")}`,303);const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.redirect(`${origin}/login?next=${encodeURIComponent("/pricing")}`,303);if(!billingConfigured())return NextResponse.redirect(`${origin}/pricing?error=${encodeURIComponent("Founder checkout is not configured yet")}`,303);const body=new URLSearchParams({mode:"subscription","line_items[0][price]":process.env.STRIPE_FOUNDER_PRICE_ID!,"line_items[0][quantity]":"1",client_reference_id:user.id,customer_email:user.email||"",success_url:`${origin}/api/billing/confirm?session_id={CHECKOUT_SESSION_ID}`,cancel_url:`${origin}/pricing`,"metadata[user_id]":user.id,allow_promotion_codes:"true"});const response=await fetch("https://api.stripe.com/v1/checkout/sessions",{method:"POST",headers:stripeHeaders(),body,cache:"no-store"});const result=await response.json();if(!response.ok||!result.url)return NextResponse.redirect(`${origin}/pricing?error=${encodeURIComponent(result.error?.message||"Unable to start checkout")}`,303);return NextResponse.redirect(result.url,303)}
