import { NextResponse } from "next/server";
import { AccountPreferencesSchema, accountPreferencesFromRow, defaultAccountPreferences } from "@/lib/account-preferences";
import { createClient, supabaseConfigured } from "@/lib/supabase/server";

async function context(){if(!supabaseConfigured())return undefined;const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();return user?{supabase,user}:undefined}
export async function GET(){const value=await context();if(!value)return NextResponse.json({data:defaultAccountPreferences});const{data,error}=await value.supabase.from("account_preferences").select("email_notifications,wishlist_alerts,valuation_research,rating_research,product_analytics,upgrade_recommendations").eq("user_id",value.user.id).maybeSingle();if(error)return NextResponse.json({error:error.message},{status:502});return NextResponse.json({data:accountPreferencesFromRow(data)})}
export async function POST(request:Request){const value=await context();if(!value)return NextResponse.json({error:"Sign in to save preferences"},{status:401});try{const input=AccountPreferencesSchema.parse(await request.json());const{error}=await value.supabase.from("account_preferences").upsert({user_id:value.user.id,email_notifications:input.emailNotifications,wishlist_alerts:input.wishlistAlerts,valuation_research:input.valuationResearch,rating_research:input.ratingResearch,product_analytics:input.productAnalytics,upgrade_recommendations:input.upgradeRecommendations,upgrade_snoozed_until:null,updated_at:new Date().toISOString()},{onConflict:"user_id"});if(error)throw error;return NextResponse.json({data:input})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Invalid preferences"},{status:422})}}
