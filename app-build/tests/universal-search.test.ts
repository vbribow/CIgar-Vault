import assert from"node:assert/strict";import test from"node:test";import{universalSearch}from"../lib/universal-search";import{buildSearchResultHref,parseRecentSearches,rememberRecentSearch,safeInternalHref}from"../lib/search-navigation";
const input={inventory:[{inventoryId:"INV-1",brand:"Cohiba",line:"Siglo",vitola:"IV",vintage:2025,currentQty:20,boxCode:"ABC MAY 25"}],collections:[{collectionId:"COL-1",name:"Fuente Legends",maker:"Arturo Fuente",expectedCigars:40,status:"Incomplete" as const}],valuations:[],wishlist:[]};
test("universal search finds inventory by cigar identity and box code",()=>{const result=universalSearch("cohiba IV",input)[0];assert.equal(result.href,"/inventory/INV-1");assert.equal(universalSearch("ABC MAY",input)[0].kind,"Inventory");const href=buildSearchResultHref(result.href,"/inventory?active=all","INV-0007");assert.equal(href,"/inventory/INV-1?searchReturn=%2Finventory%3Factive%3Dall%26vaultSearch%3DINV-0007");const restored=new URL(href,"https://hojavia.local").searchParams.get("searchReturn")||undefined;assert.equal(safeInternalHref(restored),"/inventory?active=all&vaultSearch=INV-0007");assert.equal(safeInternalHref("//outside.example/search"),undefined)});
test("universal search finds collection and workspace destinations",()=>{assert.equal(universalSearch("Fuente Legends",input)[0].kind,"Collection");assert.ok(universalSearch("purchase advisor",input).some(result=>result.href==="/decision-center"));assert.ok(universalSearch("actual factory",input).some(result=>result.href==="/learn/manufacturing-truth"))});
test("universal search requires a meaningful query",()=>{assert.deepEqual(universalSearch("c",input),[]);let recent:string[]=[];for(const query of["Cohiba","Fuente","Padron","Davidoff","Liga","OpusX"])recent=rememberRecentSearch(recent,query);assert.deepEqual(recent,["OpusX","Liga","Davidoff","Padron","Fuente"]);assert.deepEqual(rememberRecentSearch(recent," liga "),["liga","OpusX","Davidoff","Padron","Fuente"]);assert.deepEqual(parseRecentSearches("not json"),[]);assert.deepEqual(parseRecentSearches(JSON.stringify(["A","Valid","valid",7])),["Valid"])});
