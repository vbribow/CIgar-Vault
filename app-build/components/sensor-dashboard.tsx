import type { EnvironmentalSensor,Humidor,HumidorReading } from "@/lib/types";

export function SensorDashboard({sensors,humidors,readings}:{sensors:EnvironmentalSensor[];humidors:Humidor[];readings:HumidorReading[]}){
  const latest=new Map<string,HumidorReading>();
  for(const reading of [...readings].sort((a,b)=>b.recordedAt.localeCompare(a.recordedAt)))if(reading.sensorId&&!latest.has(reading.sensorId))latest.set(reading.sensorId,reading);
  const state=(sensor:EnvironmentalSensor)=>{const reading=latest.get(sensor.sensorId),humidor=humidors.find(item=>item.humidorId===sensor.humidorId);if(!reading)return sensor.connectionStatus;return humidor&&(reading.temperatureF<humidor.minTempF||reading.temperatureF>humidor.maxTempF||reading.humidity<humidor.minHumidity||reading.humidity>humidor.maxHumidity)?"Attention":"Stable"};
  return <section className="sensorDashboard" aria-labelledby="sensor-dashboard-title"><div className="sensorDashboardHead"><div><div className="eyebrow">Live humidor dashboard</div><h2 id="sensor-dashboard-title">Every sensor at a glance</h2></div><p>Current temperature, humidity, status, and last update appear together. No sensor needs to be opened individually.</p></div><div className="sensorGrid">{sensors.map(sensor=>{const reading=latest.get(sensor.sensorId),humidor=humidors.find(item=>item.humidorId===sensor.humidorId),sensorState=state(sensor);return <article className={`sensorCard ${sensorState==="Attention"?"alert":""}`} key={sensor.sensorId}><div className="sensorStatus"><i className={sensorState.toLowerCase().replace(" ","-")}/><span>{sensorState}</span></div><div className="eyebrow">{sensor.provider} · {sensor.model||"Model not set"}</div><h3>{sensor.name}</h3><p>{humidor?.name||sensor.humidorId}</p><div className="sensorReadingNow"><span><small>Temperature</small><strong>{reading?`${reading.temperatureF.toFixed(1)}°F`:"—"}</strong></span><span><small>Humidity</small><strong>{reading?`${reading.humidity.toFixed(1)}%`:"—"}</strong></span></div><div className="sensorCardMeta"><span>{sensor.syncMethod}</span><span>{reading?.batteryPercent===undefined&&sensor.batteryPercent===undefined?"Battery —":`Battery ${reading?.batteryPercent??sensor.batteryPercent}%`}</span></div><small>{reading?`Updated ${new Date(reading.recordedAt).toLocaleString()}`:sensor.lastSyncAt?`Last sync ${new Date(sensor.lastSyncAt).toLocaleString()}`:"Waiting for the first reading"}</small></article>})}</div></section>;
}
