"use client";
import { FormEvent,useState } from "react";
import type { WishlistItem } from "@/lib/types";
const today=()=>new Date().toISOString().slice(0,10);
export function WishlistPurchaseIntake({items}:{items:WishlistItem[]}){const[pending,setPending]=useState(items.filter(item=>item.status==="Purchased"&&!item.inventoryId));const[message,setMessage]=useState("");const[saving,setSaving]=useState<string>();async function convert(event:FormEvent<HTMLFormElement>,item:WishlistItem){event.preventDefault();setSaving(item.wishlistId);setMessage("");const form=new FormData(event.currentTarget);const body={wishlistId:item.wishlistId,quantity:Number(form.get("quantity")),packaging:String(form.get("packaging")||"")||undefined,vintage:String(form.get("vintage")||"")||undefined,totalCost:form.get("totalCost")?Number(form.get("totalCost")):undefined,storageLocationId:String(form.get("storageLocationId")||"")||undefined,purchaseDate:String(form.get("purchaseDate")),notes:String(form.get("notes")||"")||undefined};const response=await fetch("/api/wishlist/convert",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const result=await response.json();setSaving(undefined);if(!response.ok){setMessage(result.error||"Could not add purchase");return}setPending(current=>current.filter(value=>value.wishlistId!==item.wishlistId));setMessage(`${item.brand} ${item.vitola} added to inventory as ${result.data.inventory.inventoryId}.`)}return <section className="purchaseIntake"><div className="sectionHead"><div><div className="eyebrow">Purchase handoff</div><h2>Add purchased cigars to the vault</h2><p>Confirm the physical quantity and purchase details before inventory is created.</p></div></div>{message&&<output className="wishlistMessage">{message}</output>}<div className="purchaseForms">{pending.map(item=><form onSubmit={event=>convert(event,item)} key={item.wishlistId}><header><div><small>{item.brand} · {item.line}</small><h3>{item.vitola}</h3></div><span>Purchased</span></header><div className="purchaseFields"><label><span>Quantity *</span><input name="quantity" type="number" min="1" required/></label><label><span>Packaging</span><input name="packaging" placeholder="Box, presentation, loose sticks"/></label><label><span>Vintage / release year</span><input name="vintage"/></label><label><span>Total purchase cost</span><input name="totalCost" type="number" min="0" step=".01"/></label><label><span>Purchase date *</span><input name="purchaseDate" type="date" required defaultValue={today()}/></label><label><span>Storage location</span><input name="storageLocationId" placeholder="Humidor ID"/></label><label className="wide"><span>Purchase notes</span><input name="notes"/></label></div><button className="button" disabled={Boolean(saving)}>{saving===item.wishlistId?"Adding to vault…":"Confirm and add to inventory"}</button></form>)}{!pending.length&&<div className="emptyState">No purchased wishlist items are waiting for inventory entry.</div>}</div></section>}
